import React from "react";
import Box from "@mui/material/Box";

export default function SchemaContent() {
    return (
        <Box style={{width: '100%', height: 650, border: '1px solid gray'}}>
            test
        </Box>
    )
}